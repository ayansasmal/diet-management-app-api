# Crossplane Provider Configuration for Real AWS
# Uses IAM credentials stored in Kubernetes secret
---
apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: aws-prod
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: aws-creds-prod
      key: creds
